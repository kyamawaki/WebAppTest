@model WebAppTest.Controllers.Sample

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm("Save", "Test", FormMethod.Post))
{
    <table class="table">
        <tr>
            <th>
                @Html.DisplayName("Name")
            </th>
            <th>
                @Html.DisplayName("Value")
            </th>
            <th></th>
        </tr>

        @for(int i=0; i<Model.TestTables.Count; i++)
        {
    <tr>
        <td>
            @Html.TextBoxFor(m => Model.TestTables[i].Name)
        </td>
        <td>
            @Html.TextBoxFor(m => Model.TestTables[i].Value)
        </td>
    </tr>

        }
    </table>


    @Html.AntiForgeryToken()
    <input type="submit" />
}